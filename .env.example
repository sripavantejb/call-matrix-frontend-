# Copy to `.env` and set the URL to match backend `PORT` in backend/.env (e.g. 3000 or 3001).
# In `npm run dev`, if this file is missing, the app defaults to http://localhost:3000.
# Restart Vite after changing this.
VITE_API_URL=http://localhost:3000
